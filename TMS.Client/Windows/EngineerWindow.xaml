<Window x:Class="TMS.Client.Windows.EngineerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TMS.Client.Windows"
        mc:Ignorable="d"
        Title="EngineerWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <TabControl>
        
        <TabItem IsSelected="True">
            <TabItem.Header>
                <StackPanel>
                    <TextBlock Text="My reports"/>
                </StackPanel>
            </TabItem.Header>
            <TabItem.Content>
                <StackPanel>
                    <DataGrid ItemsSource="{Binding Path=Reports, Mode=OneWay}" IsReadOnly="True">

                    </DataGrid>
                    
                </StackPanel>
            </TabItem.Content>
        </TabItem>

        <TabItem>
            <TabItem.Header>
                <StackPanel>
                    <TextBlock Text="Create report"/>
                </StackPanel>
            </TabItem.Header>
            <TabItem.Content>
                <StackPanel>
                    <DataGrid ItemsSource="{Binding Path=Reports, Mode=OneWay}" IsReadOnly="True">

                    </DataGrid>

                </StackPanel>
            </TabItem.Content>
        </TabItem>

        <TabItem>
            <TabItem.Header>
                <StackPanel>
                    <TextBlock Text="Projects"/>
                </StackPanel>
            </TabItem.Header>
            <TabItem.Content>
                <StackPanel>
                    <DataGrid ItemsSource="{Binding Path=Projects, Mode=OneWay}" IsReadOnly="True">

                    </DataGrid>

                </StackPanel>
            </TabItem.Content>
        </TabItem>

        <TabItem>
            <TabItem.Header>
                <StackPanel>
                    <TextBlock Text="Tasks"/>
                </StackPanel>
            </TabItem.Header>
            <TabItem.Content>
                <StackPanel>
                    <DataGrid ItemsSource="{Binding Path=Tasks, Mode=OneWay}" IsReadOnly="True">

                    </DataGrid>

                </StackPanel>
            </TabItem.Content>
        </TabItem>

    </TabControl>
</Window>
